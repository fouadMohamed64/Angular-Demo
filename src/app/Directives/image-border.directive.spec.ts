import { ImageBorderDirective } from './image-border.directive';

describe('ImageBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageBorderDirective();
    expect(directive).toBeTruthy();
  });
});
